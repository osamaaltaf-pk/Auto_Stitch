# Stable Audio 3 — CPU Server Dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: PyTorch (CPU build) must be installed separately first:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Stable Audio 3 inference library
stable-audio-3 @ git+https://github.com/Stability-AI/stable-audio-3.git

# Web server
flask>=3.0.0
flask-cors>=4.0.0

# Audio / tensor utilities
torchaudio>=2.2.0
einops>=0.7.0

# HuggingFace model download
huggingface_hub>=0.23.0
safetensors>=0.4.0
